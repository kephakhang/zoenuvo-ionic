import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.youngplussoft.zoenuvo',
  appName: 'zoenuvo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
